import {
  Combobox,
  ComboboxButton,
  ComboboxInput,
  ComboboxOption,
  ComboboxOptions,
} from '@headlessui/react';

export const UiCombobox = Combobox;
export const UiComboboxButton = ComboboxButton;
export const UiComboboxInput = ComboboxInput;
export const UiComboboxOption = ComboboxOption;
export const UiComboboxOptions = ComboboxOptions;
