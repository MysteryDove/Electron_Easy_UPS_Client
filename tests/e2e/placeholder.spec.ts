import { test } from '@playwright/test';

test.skip('visual parity smoke setup pending Electron harness', async () => {
  // Placeholder test. This repo currently launches via Electron Forge,
  // so E2E browser harness wiring is intentionally deferred.
});
